# Colorado volume

Eligibility and enrollment rules for the programs a Colorado household applies for through PEAK. Skeleton: `volume.yaml` declares the programs, prefixes, and tag hierarchy; `tests/cases.yaml` holds the target household (CO-01, CO-02) with empty expectations; chapters are added one program phase at a time per `docs/design-colorado.md`.
