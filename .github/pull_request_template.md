<!--
  Items changed, Rationale, Nearest existing items, and Impact are filled in
  automatically for pull requests the app opens from the tray. If you are
  editing this by hand, fill in what you can; leave a section blank rather
  than guessing.
-->

## Items changed

<!-- Ids and identifiers of every item added or edited. -->

## Rationale

<!-- Why this item is needed, and why the nearest existing items (below) do
     not already serve. Required for new items; the app fills this from the
     item's own `rationale` field. -->

## Nearest existing items

<!-- The candidates `governance()` found closest to each new or changed item,
     and why they were not reused. -->

## Impact

<!-- Everything downstream of a changed item, per `npm run check`. -->

## Outcomes affected

<!-- Any program outcome in the impact set, or changed directly. Flag the
     program administrator(s) who own it. -->

## Checklist

- [ ] A second SME reviewed this change
- [ ] The program administrator approved it, if an outcome is affected
- [ ] Any new vocabulary terms (program, tag, type, scope) went through `volume.yaml`
